# Repair window: this workflow can repair any commit that is an ancestor of
# main's current tip (its exact tip included) for which a Release run's
# manifest artifact (release-manifest-<version>) still exists, or that an
# operator names explicitly with the source_commit input. A commit that fell
# out of main's history -- for example after a force-push or a rebase -- is
# outside that window and reconcile-guard.py refuses it regardless of what a
# manifest claims. See scripts/reconcile-guard.py for the guard this workflow
# defers to, and AGENTS.md's "Release authority" section for the approval
# reconcile still requires before it is dispatched.
name: Reconcile Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Published version to reconcile
        required: true
        type: string
      source_commit:
        description: >-
          Override the release manifest's recorded source commit (only needed
          when no manifest artifact exists for this version).
        required: false
        type: string

permissions: {}

concurrency:
  group: npm-release
  cancel-in-progress: false

jobs:
  reconcile:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Require main
        if: github.ref != 'refs/heads/main'
        run: exit 1

      - name: Check out repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Locate release manifest artifact
        id: locate
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ inputs.version }}
        run: |
          artifact=$(gh api "repos/$GITHUB_REPOSITORY/actions/artifacts?name=release-manifest-$VERSION&per_page=100" \
            --jq '[.artifacts[] | select(.expired == false)] | sort_by(.created_at) | last // empty')
          if [[ -n "$artifact" ]]; then
            echo "found=true" >> "$GITHUB_OUTPUT"
            echo "run_id=$(jq -r '.workflow_run.id' <<<"$artifact")" >> "$GITHUB_OUTPUT"
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Download release manifest
        if: steps.locate.outputs.found == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-manifest-${{ inputs.version }}
          run-id: ${{ steps.locate.outputs.run_id }}
          github-token: ${{ github.token }}
          path: manifest

      - name: Evaluate reconcile guard
        id: guard
        shell: bash
        env:
          VERSION: ${{ inputs.version }}
          SOURCE_COMMIT: ${{ inputs.source_commit }}
        run: |
          manifest_args=()
          if [[ -f manifest/manifest.json ]]; then
            manifest_args=(--manifest manifest/manifest.json)
          fi
          source_args=()
          if [[ -n "$SOURCE_COMMIT" ]]; then
            source_args=(--source-commit "$SOURCE_COMMIT")
          fi

          set +e
          decision=$(python3 -B scripts/reconcile-guard.py \
            "${manifest_args[@]}" \
            --repo . \
            --main-ref "$GITHUB_SHA" \
            --version "$VERSION" \
            "${source_args[@]}")
          status=$?
          set -e

          echo "$decision"
          if [[ $status -ne 0 ]]; then
            echo "$(jq -r '.reason' <<<"$decision")" >&2
            exit 1
          fi
          echo "source_revision=$(jq -r '.source_revision' <<<"$decision")" >> "$GITHUB_OUTPUT"

      - name: Verify immutable publication and reconcile tag
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          REQUESTED_VERSION: ${{ inputs.version }}
          SOURCE_REVISION: ${{ steps.guard.outputs.source_revision }}
        run: |
          git show "$SOURCE_REVISION:package.json" > /tmp/reconcile-package.json
          package_name=$(node -p "require('/tmp/reconcile-package.json').name")
          package_version=$(node -p "require('/tmp/reconcile-package.json').version")
          if [[ "$REQUESTED_VERSION" != "$package_version" ]]; then
            echo "Requested version does not match the reconciled commit's package.json." >&2
            exit 1
          fi

          published_version=$(npm view "$package_name@$package_version" version 2>/dev/null || true)
          published_sha=$(npm view "$package_name@$package_version" gitHead 2>/dev/null || true)
          if [[ "$published_version" != "$package_version" || "$published_sha" != "$SOURCE_REVISION" ]]; then
            echo "npm publication does not match the reconciled source revision." >&2
            exit 1
          fi

          tag_name="v$package_version"
          if git show-ref --verify --quiet "refs/tags/$tag_name"; then
            tag_sha=$(git rev-list -n 1 "$tag_name")
            tag_type=$(git cat-file -t "$tag_name")
            if [[ "$tag_sha" != "$SOURCE_REVISION" || "$tag_type" != "tag" ]]; then
              echo "Existing tag conflicts with the verified publication." >&2
              exit 1
            fi
            echo "$tag_name already reconciles the publication."
            exit 0
          fi

          tag_object=$(gh api --method POST "repos/$GITHUB_REPOSITORY/git/tags" \
            --field tag="$tag_name" --field message="Release $package_version" \
            --field object="$SOURCE_REVISION" --field type=commit --jq .sha)
          gh api --method POST "repos/$GITHUB_REPOSITORY/git/refs" \
            --field ref="refs/tags/$tag_name" --field sha="$tag_object"
